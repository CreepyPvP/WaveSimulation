// Rendering
const pixelSize = 10;
const pixelSpacing = 16;


const width = 80;

const scaleX = 20 // width / (2 * Math.PI);
const scaleY = 100;


const l = 80 / scaleX;


const globals = {};